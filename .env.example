NODE_ENV=development
DATABASE_ENVIRONMENT=development
PORT=3000
DATABASE_URL="postgresql://USER:PASSWORD@DEV_NEON_HOST/neondb?sslmode=require"
# Hostname only. Never paste a complete URL or credentials here.
PRODUCTION_DATABASE_HOST="PROD_NEON_HOST"
JWT_SECRET="min-12-chars-secret"
# Disabled by default. Enable explicitly only when the ADMIN diagnostic lab is approved.
ENABLE_PDA_SCANNER_LAB=false
# DEV/QA only. Mobile buffer-inbound screen for Hugo delivery trials. Never enable in production.
# ENABLE_HUGO_BUFFER_INBOUND=false
# Optional hint for the location selector (not mandatory). Must match an active Location.code in master data.
# HUGO_BUFFER_IN_LOCATION_PREFERENCE=
# HUGO_BUFFER_IN_WAREHOUSE_PREFERENCE=
# HUGO_BUFFER_OUT_LOCATION_PREFERENCE=
# HUGO_BUFFER_OUT_WAREHOUSE_PREFERENCE=
# DEV/QA only. Progressive operations intake form for Hugo/AVIAT definitions. Never enable in production.
# ENABLE_HUGO_OPERATIONS_FORM=false
# Local filesystem root for intake sessions (not committed). Default: data/operations-intake
# HUGO_OPERATIONS_INTAKE_DIR=data/operations-intake
# DEV/QA only. Executive read-only demo for Hugo. Never enable in production.
# ENABLE_LOGITEC_SIMPLE_PREVIEW=false
# Server-side path to official inventory Excel (never sent from browser).
# LOGITEC_DEMO_INVENTORY_XLSX_PATH=D:/DESCARGAS/INVENTARIO FISICO 14 AGOSTO 2026.xlsx
# Optional sheet override. Default: auto-detect Inventario sheet.
# LOGITEC_DEMO_INVENTORY_SHEET_NAME=Inventario Actual 14 agosto2026
# DEV only. Never set in production. Required together with CLI --confirm to run operational reset.
# ALLOW_DEV_OPERATIONAL_RESET=YES
# Enable only for the AVIAT trial and initial load. Must return to false after Hugo approves the load.
# ALLOW_TENANT_INVENTORY_RESET=false
# Playwright E2E (DEV only). Never commit real values. No fallback passwords exist in code.
# E2E_ADMIN_PASSWORD=
# QA_E2E_PASSWORD=
# E2E_CLIENT_CODE=AVIAT
